application:
  name: Restfull_books

spring:
  datasource:
    username: root
    password: root
    url: jdbc:h2:file:./book_db
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true
      path: /h2-console



